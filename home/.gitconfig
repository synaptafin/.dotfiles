[user]
	name = synaptafin
	email = synaptafin@localhost.localdomain

[ghq]
  root = ~/.ghq
[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
